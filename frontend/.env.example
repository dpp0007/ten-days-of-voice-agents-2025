# ============================================
# FRAUD ALERT VOICE AGENT - ENVIRONMENT CONFIG
# ============================================

# --------------------------------------------
# LiveKit Server Configuration (Backend)
# --------------------------------------------
# Used by the backend API to generate tokens and connect to LiveKit server
LIVEKIT_URL=http://localhost:7880
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=secret

# --------------------------------------------
# LiveKit Client Configuration (Frontend)
# --------------------------------------------
# These variables control how the browser connects to LiveKit
# IMPORTANT: Must be accessible from the device running the browser

# Option 1: Specify host and port separately (recommended)
# For local testing: localhost
# For phone on same WiFi: your computer's local IP (e.g., 192.168.1.100)
NEXT_PUBLIC_LIVEKIT_HOST=localhost
NEXT_PUBLIC_LIVEKIT_PORT=7880

# Option 2: Full URL override (takes precedence over HOST/PORT)
# Use this for ngrok or custom deployments
# NEXT_PUBLIC_LIVEKIT_URL=https://your-livekit-server.ngrok.io

# --------------------------------------------
# Optional Configuration
# --------------------------------------------
NEXT_PUBLIC_APP_CONFIG_ENDPOINT=
SANDBOX_ID=

# ============================================
# SETUP INSTRUCTIONS
# ============================================
#
# LOCAL TESTING (same computer):
# - Set NEXT_PUBLIC_LIVEKIT_HOST=localhost
# - Access at: http://localhost:3000
#
# PHONE TESTING (same WiFi):
# 1. Find your computer's IP: ipconfig (Windows) or ifconfig (Mac/Linux)
# 2. Set NEXT_PUBLIC_LIVEKIT_HOST=YOUR_IP (e.g., 192.168.1.100)
# 3. Access at: http://YOUR_IP:3000
#
# NGROK/PUBLIC ACCESS:
# 1. Tunnel LiveKit: ngrok http 7880
# 2. Set NEXT_PUBLIC_LIVEKIT_URL=https://your-ngrok-url.ngrok.io
# 3. Tunnel frontend: ngrok http 3000
# 4. Access at: https://your-frontend-ngrok-url.ngrok.io
#
# ============================================
